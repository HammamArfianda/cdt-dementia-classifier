# Notebooks

Use notebooks for exploration:

- Inspect class balance
- Preview image preprocessing
- Plot training curves
- Compare model experiments

Keep reusable training and evaluation logic in `src/cdt_classifier/`.
